<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.fouss.boardeo">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BoardActivity" />
        <activity
            android:name=".sign_in.SignInChooserActivity"
            android:label="@string/title_activity_sign_in_chooser" />
        <activity
            android:name=".sign_in.GoogleSignInActivity"
            android:label="@string/title_activity_google_sign_in"
            android:parentActivityName=".sign_in.SignInChooserActivity" />
        <activity
            android:name=".sign_in.EmailPasswordSignInActivity"
            android:label="@string/title_activity_emailpassword_sign_in"
            android:parentActivityName=".sign_in.SignInChooserActivity" />
        <activity android:name=".sign_in.SignInBaseActivity" />

    </application>

</manifest>